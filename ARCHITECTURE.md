# 架构说明

## 第一阶段：项目骨架

当前阶段刻意不提供模型调用或本地工具执行能力，而是先建立后续开发所依赖的运行边界：

```text
命令行界面 -> 配置 -> Agent 运行时 -> 模型客户端
                             |
                             +-> 本地工具
```

- `nju_agent/config.py`：读取环境变量，并校验工作区路径。
- `nju_agent/cli.py`：处理命令行参数和交互式输入输出。
- 后续的 `model.py`：封装模型厂商 API 或兼容接口的请求细节。
- 后续的 `tools/` 包：定义工具并在本地执行文件、命令等操作。
- 后续的 `agent.py`：管理对话历史、解析工具调用、控制循环终止，并处理运行异常。

在当前骨架模式下，模型 API Key 是可选的，因此项目可以在不暴露凭据的情况下启动和测试。接入模型功能后，若缺少必要配置，程序会在发起请求前给出明确的配置错误提示。
