# 架构说明

## 第五阶段：上下文管理与特色功能

当前阶段在最小 Agent 闭环中加入上下文管理、任务计划和可恢复会话：

```text
命令行界面 -> 配置 -> Agent 运行时 <-> 模型客户端
                             |
                             +-> 本地工具（文件读写、编辑、命令执行）
```

- `nju_agent/config.py`：读取环境变量，并校验工作区路径。
- `nju_agent/cli.py`：处理命令行参数和交互式输入输出。
- `nju_agent/web.py` 与 `nju_agent/web/`：提供本地 HTTP 服务和浏览器界面；通过 Server-Sent Events 实时推送工具状态，前端对 Agent 回复进行安全 Markdown 渲染。
- `nju_agent/model.py`：通过 OpenAI 兼容的 Chat Completions 接口请求模型。
- `nju_agent/tools.py`：定义并执行 `list_files`、`read_file`、`write_file`、`edit_file`、`run_command`。
- `nju_agent/agent.py`：管理对话历史、解析工具调用、控制循环终止，并处理运行异常。
- `nju_agent/context.py`：按字符预算保留关键消息，压缩较早的工具交互。
- `nju_agent/planner.py`：维护任务步骤、当前步骤和完成状态。
- `nju_agent/session.py`：以原子替换方式保存和加载本地会话消息、计划及更新时间。
- CLI 通过 Agent 工具事件回调实时显示工具调用；交互式终端使用旋转指示器，非交互输出使用普通文本。

可靠性和安全控制包括：模型请求失败自动重试；模型消息和工具调用结构校验；重复工具错误达到阈值时停止；文件路径越界拦截；危险命令拦截；命令超时；输出截断；以及不含凭据的运行日志。

上下文管理使用确定性的字符预算策略：系统提示和原始任务始终保留，优先保留最近的完整工具交互，较早交互压缩成摘要。压缩只作用于发送给模型的临时副本，不会修改持久化历史。特色功能是 `update_plan` 本地工具，模型可以创建计划、更新当前步骤并标记已完成步骤，计划状态会在后续请求中注入系统提示。会话完成后，`session.py` 保存完整消息和计划；下次通过会话 ID 恢复时，新的用户任务会追加到原历史上。Agent 默认最多执行 20 轮模型请求，同时保留重复工具失败熔断；若连续重复相同工具调用，或连续多轮只检查文件而没有运行测试，会向模型发送进展提醒。

所有文件路径都会解析并校验为工作区内路径。文件使用 UTF-8 文本读写；编辑要求旧文本恰好出现一次。命令在工作区目录执行，默认超时 30 秒，工具结果超过限制时会截断并明确标记。Agent 默认最多执行 20 步，避免模型反复调用工具导致无限循环。

浏览器附件通过本地 `/api/uploads` 接口以 Base64 文本上传。服务端只接受常见源码、配置和文档扩展名，以及 `Makefile`、`Dockerfile`；附件必须是 UTF-8 文本，单文件最大 2 MB、每轮最多 10 个。文件落在工作区内的 `.nju-agent-attachments/<随机目录>/`，聊天接口只接受该目录中的真实文件路径，并将路径提示给 Agent 通过已有的 `read_file` 本地工具读取。附件内容不发送到第三方文件服务，也不使用托管代码执行工具。

会话列表中的删除操作调用 `DELETE /api/sessions/{id}`，服务端按完整 ID 或短 ID 定位会话，并通过 `SessionStore.delete` 原子重写会话文件；删除当前会话后前端回到新对话状态。删除只影响会话记录，不会删除工作区源代码。
